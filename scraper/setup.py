from setuptools import setup

setup(name='StoutBeerNotifier',
      version='1.0',
      # packages=['licensingserver'],
      install_requires=[
          'lxml',
          'unidecode',
          'Flask'
      ],
)
